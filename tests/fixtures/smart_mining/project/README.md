# Smart Mining Offline Fixture

Public deterministic fixture for MAO classification, frontend contract, build, browser smoke, audit, and delivery-report acceptance. It uses only local mock data and contains no credentials.

Run `npm run build`, `npm run test:targeted`, `npm run test:integration`, and `npm test` from this directory.
