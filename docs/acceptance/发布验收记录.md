# MAO v0.1.0-beta.1 发布验收记录

**执行日期**：2026-07-15

**本机环境**：Windows，Python 3.11.9，Node.js 语法检查

**隐私原则**：不提交真实 Key、私人 Session、用户项目正文或本地配置。

## 六类场景

| 场景 | 执行与证据 | Token / 成本 | 结果 | 残余风险 |
|---|---|---:|---|---|
| 1. 陌生项目只读解释 | 本机 `G:\MAO_test` 历史验收先展示结构并选择性读取；只读策略与侦察覆盖由 `test_agent_permission.py`、`test_agent.py` 回归 | 历史私人 Session 不公开 | 通过，未修改目标项目 | 真实报告留在被忽略的私人 Session，不进入公开仓库 |
| 2. 故障诊断 | 本次定位 Web 启动未加载 `.env`，以 CLI 成功、Web 401、启动链代码和修复后 Web 实际请求形成证据闭环 | 修复后见 `real-provider-smoke.json` | 通过，根因已修复并补回归 | Provider 后续仍可能因 Key 过期或上游变更返回 401 |
| 3. 小功能修改 | 实现统一上下文预算与 Web 可观察性；浏览器截图、63 项定向测试、497 项全量回归 | 自动测试不调用模型 | 通过 | token 计数对非 OpenAI tokenizer 仍是保守估算 |
| 4. 双模型协作 | `python -m scripts.real_collaboration_smoke`；GLM 与 Kimi 并行只读 Worker | 输入 428，输出 200，成本 `$0.000628` | 通过；0 工具调用，0 项目写入，2 个隔离产物 | 人工付费烟雾，不在 CI 自动运行 |
| 5. 401/429/中断/Reviewer 拒绝 | 真实 Coding Plan 401 修复后成功；`test_connection_test.py`、`test_gateway_failover.py`、`test_reviewer.py` 覆盖失败收口 | 真实成功请求见 JSON；错误路径为 mock | 通过 | 真实 429 和流中断不可稳定主动制造，使用确定性 mock |
| 6. 长会话压缩恢复 | `python -m scripts.context_benchmark`；大工具输出、一次压缩、Session 保存和恢复 | 3,919 -> 796 估算 tokens；0 Provider 调用 | 通过；39 -> 8 消息，关键事实保留率 100% | 离线确定性摘要；真实摘要质量和三次压缩属于后续完整基准 |

结构化证据：

- [`context-benchmark.json`](context-benchmark.json)
- [`real-provider-smoke.json`](real-provider-smoke.json)
- [`real-collaboration-smoke.json`](real-collaboration-smoke.json)
- [`../assets/webui-chat-context.png`](../assets/webui-chat-context.png)
- [`../assets/webui-provider-context-config.png`](../assets/webui-provider-context-config.png)

## 发布工程门

- 本地全量测试：`497 passed, 1 warning`。
- Python compileall、JavaScript 语法和 `git diff --check`：通过。
- `pip-audit -r requirements.txt`：未发现已知漏洞。
- `python -m build`：wheel 与 sdist 构建成功。
- `twine check dist/*`：两个产物通过。
- 独立虚拟环境：安装、`mao --version`、`mao-ui --help` 和真实 Provider 最小请求通过；临时目录已清理。
- WebUI：`http://127.0.0.1:8128/chat` 正常，预算面板数据完整，浏览器控制台无警告或错误。

## 未完成发布门

1. 新增的 GitHub Actions 尚需推送后完成 Windows/Ubuntu、Python 3.11/3.12 首次运行。
2. README 已有真实截图，但 60-90 秒演示 GIF/视频尚未制作。
3. 完整 Context 6 的多窗口、三次压缩和真实摘要质量基准属于 Beta 后续，不阻止当前最小发布门，但不得宣传为已完成。

在前两项通过前，不创建公开 Release、不修改仓库可见性。
