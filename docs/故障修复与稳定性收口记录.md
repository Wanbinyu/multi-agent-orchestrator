# 故障修复与稳定性收口记录

**日期**：2026-07-14 至 2026-07-15
**范围**：断电后未提交工作区审计、真实使用故障补全、WebUI/CLI 体验收口、离线回归验证
**原则**：不修改 API Key，不调用真实付费模型，不提交 Git，不覆盖或清理用户会话

## 一、审计结论

- 未发现断电导致的截断文件、语法残片或 Git 索引损坏。
- `git diff --check` 通过；Python 文件可以完整编译。
- 原有修复多数已保存，但测试只覆盖单层或单 Agent 路径，无法证明真实配置和协作流程稳定。
- 本轮按真实配置形状补齐 Gateway、Worker、CLI/Web 事件和文档。

## 二、故障与根因

### 1. 流式回答重复

**现象**：流式输出一部分后连接中断，从头重试造成正文重复。

**根因**：`chat_stream()` 不区分首个 chunk 前失败和已经向用户输出后的失败。

**处理**：只有零 chunk 时允许重试；任何 chunk 已发出后立即终止并报告中断。

### 2. 429 后 fallback 不完整

**现象**：主模型配额耗尽后只能切到第一个备用模型，备用模型继续失败时无法到达第三级。

**根因**：只读取起始模型的直接 `fallback_models`，未递归读取备用模型自己的 fallback。

**处理**：递归展开并去环；冷却期模型自动跳过且发送通知。

### 3. 错误分类误伤

**现象**：`invalid max_tokens`、错误工具 Schema 等请求错误也会切换模型并进入冷却。

**根因**：用宽泛的 `invalid` / `bad request` 文本判断模型不可用。

**处理**：认证、请求参数、模型不存在/不支持、429、连接错误分别分类；请求参数错误立即暴露，不切换、不标记不健康。

### 4. 配额冷却过短

**现象**：错误明确提示 5 小时额度窗口，但 5 分钟后又会尝试主模型。

**处理**：优先读取 `Retry-After`，否则解析错误中的 hours/minutes/seconds；无法解析时最低 300 秒。

### 5. `/test-models` 诊断不可信

**现象**：命令声称失败模型会被跳过，实际未更新健康状态；Coding Plan 可能聊天成功但诊断失败；注释声称不耗额度。

**根因**：诊断绕开 Provider 正式客户端创建逻辑，并且结果没有写入健康表。

**处理**：通过 Provider 正式 `chat()` 路径发送 `max_tokens=1` 最小请求；失败写入冷却、成功清除冷却；CLI 明示会产生少量 token。

### 6. Windows 目录探查失败

**现象**：模型反复尝试 `dir` / `ls`，被命令白名单拒绝；绝对路径放进 glob pattern 时出现 `Non-relative patterns are unsupported`。

**处理**：新增 `list_dir(path)`；`glob_files(pattern, path)` 将搜索模式和绝对根目录分离；Agent 提示禁止用 shell 探查目录。

### 7. 项目任务漏判或协作后无法继续

**现象**：复杂项目有时走单模型；进入协作后 Worker 即使调用读取工具，也不会拿结果继续生成。

**根因**：协作判断完全依赖一次 LLM JSON；Worker 只有一次模型调用，没有工具循环。

**处理**：明确项目关键字先行判断；Worker 增加最多 5 轮“模型 -> 工具 -> 结果回填 -> 模型”循环，并为旧读取权限补齐目录工具。

### 8. 产生无意义 `generated_N` 文件

**现象**：模型正文代码块被自动抽取为 `generated_1.html` 等，文件名无语义，且可能把示例代码误当成项目产物。

**处理**：Agent / Worker / Reviewer 都不再自动抽取正文代码块。需要项目文件时必须显式 `write_file`；违规时先纠正一次，仍失败则保留 `content.txt` 并标记任务失败。

### 9. Worker 工具权限与协议冲突

**现象**：Worker 能在 Markdown 中调用未配置工具；原生 `tools=` 与“必须 Markdown 工具块”提示同时存在。

**处理**：执行前校验 Worker 工具白名单；原生模式只传结构化 Schema，不再注入 Markdown 工具示例。

### 10. CLI 完成项目分析后不显示最终方案

**现象**：分析 `G:\MAO_test` 时读取数十个文件，控制台最终只显示 `Read(...)`、`Update(未知文件) 0 行`、`response.md` 和 token；Java 重构方案已经生成但用户看不到。

**根因**：CLI 检测到工具块后切换到 spinner，后续 delta 仍被判定包含工具调用；收到 `done` 后只有协作分支会重新打印最终答案。

**处理**：

- `ChatStreamEvent` 增加 `tool_start` / `tool_complete` 结构化过程事件。
- 单 Agent 和协作只要使用过工具，都在 `done` 后渲染最终 `assistant_message`。
- 工具按阶段展示，每阶段展开前 4 项；结束后汇总唯一目录/文件、重复操作、成功/失败和交付文件。
- Agent 提示明确要求工具结束后给出完整结论，禁止以工具调用作为最后输出。

### 11. WebUI 桌面主区过窄与移动端裁切

**现象**：1280px 桌面下聊天中心区只有约 572px；配置页左栏和页面双重滚动。390px 手机下标题、导航、模型表和聊天输入区被裁切，上下文面板内容溢出。

**根因**：固定三栏宽度、高页头、`height:auto` 的手机聊天布局、无响应式容器的模型表，以及 `body` 隐藏横向溢出。

**处理**：

- 页头和导航总高度压缩到约 112px；桌面上下文默认收起，主对话区扩大到约 964px。
- 手机聊天使用 `100dvh`、横向会话条和上下文抽屉，输入区保持在当前视口。
- 配置页使用单一页面滚动，移动端 Provider 横向选择，模型映射转换为纵向卡片。
- 元数据强制在抽屉内换行；新建会话改为应用内对话框。
- 静态资源增加版本参数，解决浏览器继续使用旧 CSS/JS 的问题。

### 12. 项目分析目录调用过多与重复读取

**现象**：分析外部项目时需要连续调用多层 `list_dir`，同一轮还可能重复读取相同文件，增加控制台噪声和输入 token。

**处理**：

- 新增受深度和节点数限制的 `project_tree`，一次返回稳定、精简的项目结构。
- 新增零 token `/tree [路径] [深度]`，无需模型即可检查目录。
- 项目分析提示改为先树后抽样，只读取入口、依赖、配置、核心模块和测试文件。
- Agent 同步/流式路径与 Worker 增加单轮只读缓存；重复成功读取直接复用。
- 写文件、编辑、命令或未知工具执行后清空缓存，避免使用过期结果。

### 13. CLI 长回答重复显示与上下文配置误报

**现象**：使用火山方舟 Coding Plan 的 `glm-ark` 询问上下文与自动压缩时，控制台把同一段回答按“前缀不断增长”的形式重复显示；回答还错误引用 Claude 200K，并声称 MAO 不具备自动压缩。

**证据**：故障会话最终 `response.md` 只有 36 行、873 字符且内容没有重复，请求正常产生 token、费用和 `completed` 工程记录。因此不是模型重复生成、鉴权失败或 Provider 流式 delta 拼接错误。

**根因**：

- CLI 对每个 delta 累加 `final_content`，再把完整累计 Markdown 交给 Rich `Live`。
- `vertical_overflow="visible"` 在内容超过终端高度后无法可靠清除历史帧，导致每次累计渲染都进入滚动记录。
- MAO 没有把模型映射、上下文预算和压缩阈值提供给模型；模型只能根据通用知识猜测，并把 Anthropic 兼容协议误解为 Claude 模型环境。

**处理**：

- Rich Live 改为 `vertical_overflow="ellipsis"` 和 `transient=True`，只承担有界临时预览。
- Live 停止后统一打印一次最终正文；工具和协作结果仍使用结果面板。
- Agent 新增结构化上下文状态，报告模型别名、Provider、上游请求模型 ID、当前 token 估算、预算来源和压缩阈值。
- 新增零模型调用的 `/context` 命令；当前未显式配置模型窗口时，显示 Agent 默认 32K 预算和约 24K（75%）压缩阈值。
- 系统提示注入静态运行时事实，并明确 `anthropic` 只是 API 兼容协议，不代表当前模型是 Claude。
- 增加流式最终正文唯一性、Live 配置、上下文默认/显式预算、系统提示事实和 `/context` 输出回归测试。

### 14. 任务词误授权与同步 approve 静默写入

**现象**：旧逻辑只有“只做方案/不要修改”等少量关键词会进入只读边界；普通解释、诊断和审查在 auto 模式下仍可能收到写工具。同步接口又无法产生交互批准，却把 approve 模式按自动批准执行。

**风险**：问句中的“未来是否调整”“有没有需要优化”可能被简单关键词误认为修改请求；同步调用可能绕过用户期望的批准步骤。

**处理**：

- 新增 Phase 7.1 `TaskIntentClassifier` 和任务策略矩阵，零模型调用完成分类。
- 只有明确祈使表达的 change/build 允许项目写入；问句中的写入词不构成授权。
- answer/explain/diagnose/review/plan/monitor 和未分类请求强制只读工具。
- 短续接请求继承上一轮意图，缺少可靠历史时保守只读。
- 同步 approve 路径拒绝非读工具，要求使用流式批准或显式切换 auto。
- RunJournal、CLI 和 Web 显示类型、风险与写入状态；真实 401 失败路径仍保留分类结果。

## 三、自动验证证据

```bash
python -m compileall -q src tests scripts
python -m pytest -q tests/test_gateway_failover.py tests/test_connection_test.py
python -m pytest -q tests/test_worker.py tests/test_worker_e2e.py
python -m pytest -q tests/test_chat_router_stream.py tests/test_chat_command_mode.py
python -m pytest -q tests/test_agent.py tests/test_agent_stream.py tests/test_chat_command_mode.py tests/test_chat_command_output.py
python scripts/demo_failover.py
python -m pytest -q
```

当前全量结果：`442 passed, 1 warning`。警告来自 Starlette `TestClient` 对 `httpx` 的弃用提示，不是业务失败。

WebUI 另在 `1280x720` 和 `390x844` 下完成配置页、聊天页、上下文抽屉和新建会话交互验收；最终页面无横向溢出，浏览器控制台无错误。

## 四、真实项目分析验证

- 已用 CLI `/tree G:\MAO_test 4` 验证 39 个节点的本地项目树，零模型调用、零 token。
- 原故障请求误入多模型协作，成本 `$0.108996`，并在 Reviewer 未通过后因 Rich 标签不匹配崩溃。
- 修复协作路由和 Rich 输出后，进一步发现模型会无视提示读取 20 个以上文件、输出在章节中途截断。
- 最终将边界下沉到执行器：分析型请求只暴露只读工具、最多读取 12 个不同文件、超长结果无工具压缩。
- 最终真实请求成本 `$0.017799`，相对原故障下降约 84%；保存结果 3064 字符，包含项目结构、发现、迁移阶段、风险和验收并完整结束。

## 五、仍未执行的真实外部验证

- 未调用 `/test-models`，避免无人值守时消耗真实模型额度。
- 未使用真实 429 响应；通过与实际错误文本一致的 mock 验证 5 小时冷却和三级切换。
- MCP 适配器代码与 mock 测试已完成，但当前环境未安装可选 `mcp` 包，未连接真实 Server。

## 六、剩余限制

1. 模型健康状态仅保存在当前进程内，重启后清空。
2. 一次性非流式任务能自动 fallback，但不像流式 CLI/Web 那样实时展示切换卡片；可从响应模型和日志判断。
3. Worker 工具循环上限为 5，超限时要求模型直接收口；极大型项目仍应拆成更多子任务。
4. 协作采用前置整体批准，Worker 内部写文件不再逐个弹窗，这是当前权限设计。
5. 命令执行仍是白名单机制，不是容器级沙箱。
6. 单轮重复读取已缓存；跨会话仍需依赖现有项目索引，尚未实现按项目根目录自动复用结构快照。

## 七、后续人工验收建议

在额度允许且有人观察时执行：

1. CLI `/test-models`，确认失败模型进入冷却并显示少量 token 提示。
2. 使用一个真实三级 fallback 配置触发 429，确认 CLI 和 Web 依次显示切换。
3. 对外部目录发起“探查、设计、实现”项目任务，确认 Worker 先列目录/读文件，再用明确文件名写入。
4. 检查输出目录不存在新增 `generated_N`，失败任务保留 `content.txt` 供排查。
5. 对包含 30 个以上源码文件的项目执行“分析结构并给出重构方案”，确认 CLI 同时显示阶段进度、最终方案、本轮工作和交付文件。

真实接口脚本有防误触保护，必须显式执行 `python scripts/test_failover.py --live`；离线演示使用 `python scripts/demo_failover.py`。
