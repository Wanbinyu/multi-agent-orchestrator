# MAO 项目进度与关键操作

**用途**：跨设备、跨工具继续开发时的单一状态入口

**更新时间**：2026-07-16

## 1. 当前状态

| 项目 | 状态 |
|---|---|
| 公开版本 | `v0.1.0-beta.2` pre-release |
| 仓库 | Public：<https://github.com/Wanbinyu/multi-agent-orchestrator> |
| 当前分支 | `main` |
| B3.1 起始提交 | `67ac9a9` |
| 自动测试基线 | `556 passed, 1 warning` |
| CI | B3.4 已通过 Windows/Ubuntu、Python 3.11/3.12、依赖审计和密钥扫描；B3.5 尚未提交触发 |
| 当前开发阶段 | `v0.1.0-beta.3` 进行中，B3.1-B3.5 本地验收已完成 |
| 当前第一任务 | 审阅 B3.5 未提交差异，确认后提交并等待 CI |

## 2. 已完成重点

- Provider 配置 UI、CLI/Web 对话和流式响应。
- 多模型 Orchestrator/Dispatcher/Worker/Reviewer 协作。
- `auto` / `approve` / `readonly` 权限模式。
- 工具注册表、原生工具双轨、MCP、Hooks 和本地模型。
- Evidence、VerificationGate、RequirementCheck、CompletionAudit 和 RunJournal。
- 上下文动态预算和最小长会话基准。
- `pipx` 安装、`mao` 和 `mao web` 全局入口。
- 公开 `v0.1.0-beta.2`，旧阶段文档已归档。
- OpenCode 参考审计和新的 Beta 路线图。
- 已确定 `beta.3` 至 `beta.6` 的版本边界、Claude 官方 API 边界和 Plugin API v0 时机。
- 已建立当前版本执行清单、跨设备恢复步骤和文档单一入口。
- B3.1 已建立 Provider 能力真值字段、保守启用规则和配置往返保存，旧配置保持兼容。
- B3.2 已按 Anthropic 官方资料修正模型真值和连接错误分类；真实付费 smoke 未授权、未执行。
- B3.3 已完成 Anthropic 结构化工具回合：同步/流式状态、原始工具 ID、结果顺序、人工批准写入、失败 Evidence、thinking 私有态和压缩边界均通过离线验收。
- B3.3 未调用真实付费 API；`tool_use` 与 `vision` 能力仍保持 `unverified`，分别等待真实端到端 smoke 和结构化图片消息。
- B3.4 已统一 ProviderError、连接测试、同步/流式重试与故障切换、CLI/Web 错误语义和 RunJournal Provider Evidence。
- B3.4 区分短期限流与长期配额，认证/配置错误不会进入自动切换；所有错误展示均使用脱敏错误码和操作建议。
- B3.5 已实现 Hook/MCP 有界脱敏诊断、坏条目隔离、CLI 摘要和 Web 独立诊断接口；无扩展配置仍保持安静。
- B3.5 修复 Windows 无控制台首次启动误入 Questionary 的问题，并新增 wheel/sdist、隔离安装、空目录 CLI/Web 的自动发行验收。
- B3.5 本地全量回归为 `556 passed, 1 warning`，发行包与 `/health` 验收通过；改动按要求保留在工作树，尚未提交或推送。

## 3. 当前未完成重点

按顺序：

1. `beta.3` Provider/Claude 可信接入与首次使用稳定性。
2. `beta.4` 工程透明度、会话恢复和长任务上下文。
3. `beta.5` 路由、执行深度和真实 token/完成率基准。
4. `beta.6` Plugin API v0。

详细目标见 [`版本计划-v0.1.0-beta.3至beta.6.md`](版本计划-v0.1.0-beta.3至beta.6.md)。

## 4. 新设备恢复步骤

```bash
git clone https://github.com/Wanbinyu/multi-agent-orchestrator.git
cd multi-agent-orchestrator
git status --short --branch
git pull --ff-only
python -m venv .venv
```

激活环境后：

```bash
python -m pip install -e ".[test]"
python -m pytest -q
mao --version
mao web --help
```

不要复制旧电脑的 `.env` 到公开位置。需要的 Provider Key 在新设备本地重新配置。

## 5. 每次开始工作的固定步骤

```bash
git status --short --branch
git fetch origin
git rev-list --left-right --count HEAD...origin/main
```

然后：

1. 阅读本文和当前版本执行清单。
2. 确认工作树中的改动来源，不回滚不属于当前任务的修改。
3. 只把一个步骤标记为进行中。
4. 修改前记录验收标准和针对性测试。
5. 修改后先跑针对性测试，再按风险扩大回归。

## 6. 常用运行命令

安装用户：

```bash
pipx install git+https://github.com/Wanbinyu/multi-agent-orchestrator.git
mao
mao web
pipx upgrade multi-agent-orchestrator
pipx uninstall multi-agent-orchestrator
```

源码开发：

```bash
python -m pip install -e ".[test]"
mao
mao web --no-open
```

一次性任务：

```bash
mao run "检查当前项目并给出风险"
```

## 7. 验证命令

```bash
python -m pytest -q
python scripts/verify_distribution.py
python -m compileall -q src tests scripts run.py
node --check src/ui/static/js/app.js
node --check src/ui/static/js/chat.js
git diff --check
python -m pip_audit
```

构建发布包：

```bash
python -m build --wheel --sdist
python -m twine check dist/*
```

真实 Provider smoke 不属于自动 CI；调用付费模型前必须确认 Key、模型、预计次数和费用边界。

## 8. 文档更新规则

每完成一个 Beta 子任务，同时更新：

- 本文的当前状态和第一任务。
- 对应版本执行清单的复选框、测试结果和剩余风险。
- 影响架构时更新 `MAO-架构概览.md`。
- 改变版本目标时更新版本计划，不在多个旧计划重复维护。
- 发布时更新 CHANGELOG 和不可变 Release Notes。

已完成的整阶段计划移入 `docs/archive/`，但当前执行清单在版本发布前不能归档。

## 9. 安全操作

- Key 只放本地 `.env`，不得写入 Markdown、Issue、日志或命令输出。
- 曾在对话中出现的 Key 应视为泄露并在 Provider 控制台轮换。
- 不运行来自未知仓库的安装脚本或插件。
- 不自动改 GitHub 可见性、Tag 或 Release；这些操作需要所有者单独确认。
- 不对用户已有 Git 改动执行 reset、checkout 或覆盖写入。

## 10. 当前继续入口

下一次执行时先运行 `git diff --check` 和 `git diff --stat` 审阅 B3.5 未提交差异；确认后提交 `fix: expose extension diagnostics and first-run checks` 并等待远端 Windows/Linux CI。CI 通过后在 [`Beta3-执行清单.md`](Beta3-执行清单.md) 勾选最后一项，再进入 **B3.6 发布收口**。
