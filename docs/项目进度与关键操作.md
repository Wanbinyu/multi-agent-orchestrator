# MAO 项目进度与关键操作

**用途**：跨设备、跨工具继续开发时的单一状态入口

**更新时间**：2026-07-21

## 1. 当前状态

| 项目 | 状态 |
|---|---|
| 公开版本 | [`v0.1.0-beta.6`](https://github.com/Wanbinyu/multi-agent-orchestrator/releases/tag/v0.1.0-beta.6) pre-release（Tag/Release 待所有者确认） |
| 仓库 | Public：<https://github.com/Wanbinyu/multi-agent-orchestrator> |
| 当前分支 | `main` |
| B3.1 起始提交 | `67ac9a9` |
| 自动测试基线 | `853 passed, 1 warning` |
| CI | beta.6 候选提交 `998ea25` 通过 Windows/Ubuntu × Python 3.11/3.12、pip-audit 和 gitleaks（[run 29841673789](https://github.com/Wanbinyu/multi-agent-orchestrator/actions/runs/29841673789)） |
| 当前开发阶段 | `v0.1.0-beta.6` 发布收口完成（本地）；Tag/Release 待所有者确认；B5.4 真实评测仍暂停 |
| 当前第一任务 | 等待所有者确认 beta.6 Tag/GitHub pre-release；之后按 `v0.2.0` 进入条件推进。保持真实 Provider 调用暂停 |

## 2. 已完成重点

- Provider 配置 UI、CLI/Web 对话和流式响应。
- 多模型 Orchestrator/Dispatcher/Worker/Reviewer 协作。
- `auto` / `approve` / `readonly` 权限模式。
- 工具注册表、原生工具双轨、MCP、Hooks 和本地模型。
- Evidence、VerificationGate、RequirementCheck、CompletionAudit 和 RunJournal。
- 上下文动态预算和最小长会话基准。
- `pipx` 安装、`mao` 和 `mao web` 全局入口。
- 公开 `v0.1.0-beta.2`，旧阶段文档已归档。
- OpenCode 参考审计和新的 Beta 路线图。
- 已确定 `beta.3` 至 `beta.6` 的版本边界、Claude 官方 API 边界和 Plugin API v0 时机。
- 已建立当前版本执行清单、跨设备恢复步骤和文档单一入口。
- B3.1 已建立 Provider 能力真值字段、保守启用规则和配置往返保存，旧配置保持兼容。
- B3.2 已按 Anthropic 官方资料修正模型真值和连接错误分类；真实付费 smoke 未授权、未执行。
- B3.3 已完成 Anthropic 结构化工具回合：同步/流式状态、原始工具 ID、结果顺序、人工批准写入、失败 Evidence、thinking 私有态和压缩边界均通过离线验收。
- B3.3 未调用真实付费 API；`tool_use` 与 `vision` 能力仍保持 `unverified`，分别等待真实端到端 smoke 和结构化图片消息。
- B3.4 已统一 ProviderError、连接测试、同步/流式重试与故障切换、CLI/Web 错误语义和 RunJournal Provider Evidence。
- B3.4 区分短期限流与长期配额，认证/配置错误不会进入自动切换；所有错误展示均使用脱敏错误码和操作建议。
- B3.5 已实现 Hook/MCP 有界脱敏诊断、坏条目隔离、CLI 摘要和 Web 独立诊断接口；无扩展配置仍保持安静。
- B3.5 修复 Windows 无控制台首次启动误入 Questionary 的问题，并新增 wheel/sdist、隔离安装、空目录 CLI/Web 的自动发行验收。
- B3.5 已提交（`2f9ee73`）；远端 CI 首轮因托管环境 setuptools<77 不识别 PEP 639 license 失败，`ebd391a` 提升构建要求后矩阵全绿。
- 预设模型目录扩充：`src/models/catalog.py` 成为 CLI 与 Web 预设的单一真值源，新增 GPT-5、DeepSeek V4 Pro/Flash、Kimi K2.7 系列、GLM-5、Qwen3 Coder、MiniMax M2.7、豆包 Seed 2.1 Pro 和 Gemini 3.x；未核实条目保持 `unverified`，价格仅为占位。
- 版本计划已把 beta.4（干扰度指标、L0/L1/L2 分层、Reviewer 信息限制验证）和 beta.5（预设扩充、基准防污染、对抗式测试生成、本地模型成本档）的新方向写入范围与发布门。
- 使用反馈修复了权限模式双重门控：`auto` 可直接写入/执行，`approve` 只确认非只读工具，`readonly` 可读取但拒绝写入/命令；未知任务跟随会话模式且不误触发工程验证，“帮我创建好”可直接进入构建任务。全量回归 `589 passed, 1 warning`。
- 已吸收 Grok Build 的基础行为契约：层级项目规则、`deny/ask/allow` 权限规则、持久化 Plan 状态和无工具四角色多模型 Planning Council；主 Agent/Worker 共用执行边界，CLI/Web 均可进入、查看、修订、批准实施和取消。详细设计见 [`Grok-Build行为契约融合.md`](Grok-Build行为契约融合.md)。
- 已完成 Grok Build、Codex、OpenCode、Aider、Cline、Goose、Qwen Code 的许可证与行为契约审计；权限规则已先行吸收 `justification` 和 `match/not_match` 加载期自检，失败规则自动排除。全量回归 `621 passed, 1 warning`。后续 JSONL、检查点、Repo Map 和 OS 沙箱严格按稳定性阶段接入，详见 [`开源Coding-Agent参考与吸收计划.md`](开源Coding-Agent参考与吸收计划.md)。
- 已完成智慧矿区真实任务复盘：当前生成项目通过 TypeScript 与 Vite 内存构建，但 Mock 请求架构导致登录、数据加载和页面切换失败；原始构建请求被误判为 `unclassified`，33 个项目文件写入后仍 `audit: not_required`。后续稳定性切片见 [`真实任务稳定性改进计划.md`](真实任务稳定性改进计划.md)。
- B4.S1 已完成：真实自然语言构建请求进入 `build/high/deep`；RunJournal v3 新增初始/有效意图和 observed mutation。单文件写入触发 standard 审计，多文件、依赖或新目录触发 deep 审计，Session 根 `response.md` 排除，且动态升级不扩大执行权限。全量回归 `633 passed, 1 warning`。
- B4.S2 已完成：新增只读项目命令发现、结构化 cwd、参数数组执行、命令轨迹、输出截断、Vite 临时构建和一次纠错上限；脚本引用缺失依赖时标记不可用，预检拒绝不再生成虚假测试门。全量回归 `653 passed, 1 warning`。
- B4.S3 已完成：高风险前端构建固定拆分架构/脚手架、页面、数据/API、集成与 Reviewer 职责；结构化合同覆盖入口、路由、依赖、所有权、验证命令和 smoke 路径。集成 Worker 确定性检查缺页、错误 import 和依赖闭包，并要求真实成功命令证据；Reviewer 不可覆盖失败。RunJournal 可见职责与实际模型。全量回归 `664 passed, 1 warning`。
- B4.S4 已完成：新增受控 Playwright smoke、动态本地端口和进程树清理；桌面/移动端检查登录、七个主路由、Mock 数据、非空图表、console/page error、横向溢出与声明式遮挡。真实正夹具通过，错误 Mock 夹具稳定 blocked。全量回归 `673 passed, 1 warning`。
- B4.S5 已完成：新增本会话/今日 RunJournal 真实报告，区分创建、修改、验证、待办、用户步骤与风险并保留 provenance；CLI、Web 和明确自然语言报告请求共用零 Provider 路径。新增职责模型 token/成本、成功率、首轮可运行率、返工、误诊和 token/有效交付指标。全量回归 `679 passed, 1 warning`。
- B4.S6 已完成：公开脱敏智慧矿区夹具离线串联分类、多模型前端合同、闭包、真实 npm 命令、Playwright、完成审计和报告，CI 全程零 Provider 调用；正常样例完成，错误 Mock 与缺失路由样例稳定 blocked。同步修复 Windows `npm.CMD` 解析和首轮可运行指标。全量回归 `683 passed, 1 warning`。
- B4.3 已完成：最新 running/blocked/未完成计划会触发本地恢复确认；CLI `/resume` 和 Web 横幅在确认前阻断同步/流式消息。继续或放弃只记录本地 decision，不调用模型、不执行工具、不回滚；续跑创建新 run 并一次性携带未完成步骤检查点。全量回归 `689 passed, 1 warning`，桌面/390px 浏览器交互通过。
- B4.4 已完成：压缩升级为 L0 artifact 索引、L1 结构化摘要、L2 近期全文；加入安全去重、JSON Schema/纯文本回退、关键实体修复、RunJournal 固定 checkpoint、实体保留率和相关 token 指标。32K/64K/128K/200K 各三次离线压缩全部通过，事实保留率 1.0、Provider 调用 0。全量回归 `695 passed, 1 warning`。
- B4.5 已完成：项目索引 v2 持久化 root、树路径、文件摘要与 SHA-256；未变项目二次刷新零内容读取，单文件变化只读该文件，hash 相同只更新元数据。树和搜索优先使用增量索引，损坏/跨根安全重建，搜索可显式绑定绝对项目路径。全量回归 `701 passed, 1 warning`。
- B4.6 已完成：Reviewer 默认 restricted，只看需求、计划、文件、真实工具/验证/需求/审计证据，不含 Worker 正文；配置可切 full。实际模式写入 collaboration metrics，确定性审计和失败 Worker 仍可否决 Reviewer。全量回归 `703 passed, 1 warning`。
- B4.7 已发布：版本为 `0.1.0b4`，完整测试集合 `722 passed, 1 warning`；权限确认、恢复矛盾状态、原生工具压缩、Reviewer 类型、任务扇出、前端假阳性、报告跨会话事实和 Web 并发均已加固。真实任务脱敏回放、四档三次上下文压缩、wheel/sdist、`twine check`、干净虚拟环境安装、空目录 CLI、Web `/health`、pip-audit 和 gitleaks 均通过，且未调用付费 Provider。跨平台修复提交 `c0caecb` 后，远端 CI 五组任务全绿。
- B5.1 已在本地完成：通用 benchmark Schema 和策略协议让单模型/MAO 共用隔离 runner 与确定性验收；公开六类程序化任务三次运行形成 36 个稳定结果，Provider 调用 0。JSON/Markdown 报告记录 token、成本、工具、总耗时、完成率、误修改率和验证通过率，并把 fixture 数据标记为 `synthetic_contract`。wheel/sdist、`twine check` 和公开 benchmark 归档合同通过；全量集合 `735 passed, 1 warning`，远端 CI 等待提交后运行。
- B5.2 已在本地完成：RunJournal v4 记录 `fast/standard/deep` 的请求、建议、实际值、理由和预算；三个档位约束主 Agent/Worker 工具轮次、上下文比例、Worker 并发、协作 Reviewer 与变更验证下限。CLI `/depth` 和 Web 会话 API 可持久化显式选择，高风险与真实多文件写入不可被 `fast` 绕过。完整回归 `749 passed, 1 warning`，工程 benchmark 36/36 稳定、智慧矿区正反回放和分发验收通过，Provider 调用 0。
- B5.3 已在本地完成：路由只读取任务、已验证能力、可追溯价格、上下文、健康状态和用户约束；自动升级最多一次，未知能力/价格不参与能力或节省声明，`/routing fixed` 可锁定主模型。自动候选可切换失败时优先回退用户主模型，完整候选审计写入 RunJournal v5，CLI/Web 显示简洁原因。完整回归 `763 passed, 1 warning`，工程 benchmark 36/36 稳定、智慧矿区正反回放和分发验收通过，Provider 调用 0。
- B5.4 已加入外部能力评测提醒：到达阶段时先提示所有者开始测试，确认模型、次数、费用和公开范围；第一目标为 Terminal-Bench/Harbor adapter，稳定后再评估 SWE-bench Lite/Verified，Aider 只作代码编辑补充。
- B5.4 离线基础设施已完成：同一 harness 用独立 profile 表示 `fixed-single`/`auto-route`/`multi-model`，6 类任务三次共 54 条合成结果稳定，Provider 调用 0。
- 新增生产 Agent 链的 `mao benchmark-agent` 无交互入口、受限 `allowed_models`、所有策略共享的调用/费用停止门，以及 Harbor `BaseInstalledAgent` adapter。真实 Key 未读取，真实 Provider 调用仍为 0。
- B5.4 操作、密钥注入、可重现安装和结果解读见 [`B5.4-真实能力评测操作手册.md`](B5.4-真实能力评测操作手册.md)。
- B5.4 当前完整回归 `787 passed, 1 warning`，分发验收通过。开发机仅有 Python 3.11，所以要求 Python 3.12 的 Harbor `0.20.x` 真实 import/Docker smoke 尚未运行。
- B5.4 private 真实 smoke：`fixed-single/glm-ark` 外部 verifier 通过，3 次调用、`2857/431` token、`$0.003288`；索引重定向后 `auto-route` 也通过外部 verifier，但保守只使用 `glm-ark`。
- 所有者将累计尝试上限由 20 提高到 35、费用上限保持 `$0.20`。`multi-model` 先后暴露顶层数组、角色别名、列表型验收标准和软件 Worker 语义漂移，均已修复并加入离线测试。
- 旧 `LiveBenchmarkSpendGuard` 在整轮结束后才统计次数，最后一轮从剩余 8 次越过到 13 次，导致累计 **40/35**、成本 `$0.032453`。真实调用已停止；次数门已下沉为每个 Provider 请求前的线程安全硬预占，mock 验证同步、流式和 100 次并发争用均不会让第 N+1 次到达 Provider。
- 软件计划归一化会修正常见未注册角色和创作角色漂移；通用别名变更会保留已配置的合法模型，只有软件任务误用 `writer/editor` 等创作角色时才切换到软件 Worker 默认模型。
- 最近 `multi-model` 报告为 `bench-fb6c0b3bf4d0`：只实际使用 `glm-ark`，外部 verifier 未通过，因此 B5.4 多模型真实能力仍未验收。未获得新的所有者授权前不得继续真实重试。
- B5.5 新增默认关闭的只读对抗测试角色：仅在显式启用的 `deep change/build` 协作、Worker 全部成功且确定性审计通过后运行；只读取直接证据，不能修改项目。`refuted` 只能把完成降级为 blocked，`inconclusive` 只增加剩余风险，任何结果都不能绕过确定性完成审计。
- CLI `/adversarial on|off` 与 Web 会话开关共享持久化状态，运行中修改返回 409。Web 已实测默认关闭、刷新持久化、320/390/1280px 无重叠且控制台无错误。
- 本地/Ollama 的零边际成本只作为路由评分因素，不能绕过健康冷却、已验证能力和上下文容量；`deep build` 所需 reasoning 不满足时仍升级到合格云模型。
- B5.5 完整回归 `798 passed, 1 warning`；工程基准 54/54 稳定、Provider 调用/尝试 0，分发验收通过。实验开关的真实模型效果尚未声明，也未新增真实调用。
- B5.6 发布收口已完成（本地）：模型目录单一真值审计通过并修复 CLI `ark` 预设漂离 catalog 的小问题，新增防漂移回归测试；公开基准来源/生成/运行/结果可复现并标记 `synthetic_contract`；版本号升至 `0.1.0b5`（`src/version.py`、`pyproject.toml`、`tests/test_version.py` 同步）；CHANGELOG、Release Notes、升级说明完成；全量回归 `799 passed, 1 warning`、pip-audit 无已知漏洞、compileall/JS 语法/diff 检查通过。复核中发现 `cbcf056`（B5.5）远端 CI 实际失败但此前未记录：`build/` gitignore 规则误藏基准 `tasks/build/` 夹具，文件未提交导致 CI 失败而本地通过；`737ac8e` 锚定 `/build/` 并补齐夹具后修复，远端 CI 全绿（含 gitleaks 8.24.3）。提交序列：`814bec9` catalog 修复、`6a496fb` 版本与发布文档、`737ac8e` gitignore 与夹具修复、`6f95f27` 记录 CI 绿。Tag `v0.1.0-beta.5`（`6f95f27`）与 GitHub pre-release 已由所有者确认并创建。
- `v0.1.0-beta.6` Plugin API v0 已完成（本地）：B6.1 契约（`src/plugins/api.py`：`PluginManifest`/`PluginContext`/`Plugin` 协议、`MAO_PLUGIN_API_VERSION="0.1"`、能力/权限白名单、回滚）；B6.2 管理器（`src/plugins/manager.py`：entry point 发现、版本拒绝、启用门控、隔离加载、shutdown、诊断）；B6.3 CLI `mao plugin list/doctor/enable/disable` + 启动接线（`src/plugins/runtime.py`）；B6.4 示例插件 `examples/plugins/mao_wordcount_plugin`（独立可安装包 + entry point）；B6.5 Web `/api/plugins` + chat「插件」只读标签。`ToolRegistry`/`HookRegistry` 增注销方法支持隔离回滚。版本号 `0.1.0b6`。全量回归 `853 passed, 1 warning`、pip-audit 无漏洞、`verify_distribution.py` 含示例插件发现/启用/执行/关闭通过、compileall/JS/diff 通过。Tag/GitHub pre-release 待所有者确认。

## 3. 当前未完成重点

按顺序：

1. 所有者确认 `v0.1.0-beta.6` Tag 和 GitHub pre-release（不自动执行）。
2. `beta.5` B5.4 真实多模型对比；只在所有者重新授权累计调用边界后恢复。
3. `v0.2.0` 进入条件：外部用户、可复现真实基准、Plugin API 兼容策略。

详细目标见 [`版本计划-v0.1.0-beta.3至beta.6.md`](版本计划-v0.1.0-beta.3至beta.6.md)。

## 4. 新设备恢复步骤

```bash
git clone https://github.com/Wanbinyu/multi-agent-orchestrator.git
cd multi-agent-orchestrator
git status --short --branch
git pull --ff-only
python -m venv .venv
```

激活环境后：

```bash
python -m pip install -e ".[test]"
python -m pytest -q
mao --version
mao web --help
```

不要复制旧电脑的 `.env` 到公开位置。需要的 Provider Key 在新设备本地重新配置。

## 5. 每次开始工作的固定步骤

```bash
git status --short --branch
git fetch origin
git rev-list --left-right --count HEAD...origin/main
```

然后：

1. 阅读本文和当前版本执行清单。
2. 确认工作树中的改动来源，不回滚不属于当前任务的修改。
3. 只把一个步骤标记为进行中。
4. 修改前记录验收标准和针对性测试。
5. 修改后先跑针对性测试，再按风险扩大回归。

## 6. 常用运行命令

安装用户：

```bash
pipx install git+https://github.com/Wanbinyu/multi-agent-orchestrator.git
mao
mao web
pipx upgrade multi-agent-orchestrator
pipx uninstall multi-agent-orchestrator
```

源码开发：

```bash
python -m pip install -e ".[test]"
mao
mao web --no-open
```

一次性任务：

```bash
mao run "检查当前项目并给出风险"
```

## 7. 验证命令

```bash
python -m pytest -q
python scripts/verify_distribution.py
python -m compileall -q src tests scripts run.py
node --check src/ui/static/js/app.js
node --check src/ui/static/js/chat.js
git diff --check
python -m pip_audit
```

构建发布包：

```bash
python -m build --wheel --sdist
python -m twine check dist/*
```

真实 Provider smoke 不属于自动 CI；调用付费模型前必须确认 Key、模型、预计次数和费用边界。

## 8. 文档更新规则

每完成一个 Beta 子任务，同时更新：

- 本文的当前状态和第一任务。
- 对应版本执行清单的复选框、测试结果和剩余风险。
- 影响架构时更新 `MAO-架构概览.md`。
- 改变版本目标时更新版本计划，不在多个旧计划重复维护。
- 发布时更新 CHANGELOG 和不可变 Release Notes。

已完成的整阶段计划移入 `docs/archive/`，但当前执行清单在版本发布前不能归档。

## 9. 安全操作

- Key 只放本地 `.env`，不得写入 Markdown、Issue、日志或命令输出。
- 曾在对话中出现的 Key 应视为泄露并在 Provider 控制台轮换。
- 不运行来自未知仓库的安装脚本或插件。
- 不自动改 GitHub 可见性、Tag 或 Release；这些操作需要所有者单独确认。
- 不对用户已有 Git 改动执行 reset、checkout 或覆盖写入。

## 10. 当前继续入口

`v0.1.0-beta.6` 发布收口已在本地完成：Plugin API v0 契约、管理器、CLI、示例插件与 Web 可见性均已落地；版本号 `0.1.0b6`、CHANGELOG/Release Notes/升级说明完成；全量回归 `853 passed, 1 warning`、pip-audit 无漏洞、`verify_distribution.py` 含示例插件发现/启用/执行/关闭通过、compileall/JS/diff 通过。Tag 和 GitHub pre-release 仍需所有者单独确认，不自动执行。确认后按 `v0.2.0` 进入条件推进；B5.4 真实多模型评测仍单独暂停，只有所有者重新给出累计次数边界后才允许继续 private smoke。详见 [`Beta6-执行清单.md`](Beta6-执行清单.md)。
